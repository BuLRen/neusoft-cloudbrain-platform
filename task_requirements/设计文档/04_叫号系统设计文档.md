# 熙康云医院 · 叫号系统设计文档

> **文档目的**：在动手之前，把"医生叫号 → 候诊大屏更新 → 患者应答进诊室"这条链路的设计方案、数据模型、接口、实施步骤全部讲清楚。
>
> **范围**：医生工作站 / 候诊大屏 / 报到机联动 / 后端状态机 / 数据库迁移 / 实时通信（WebSocket）。

---

## 一、为什么这件事"挺麻烦"

叫号系统本质上不是"再加一个 CRUD"，而是一个**跨多端、有状态、实时同步**的分布式小系统。它要回答 4 个问题：

1. **谁是号源？** —— 来自挂号 + 报到（已实现）
2. **谁来叫号？** —— 医生在医生工作站点"叫下一个"
3. **叫了之后谁知道？** —— 候诊大屏、报到机、患者手机，所有端要**实时**看到"当前叫到 X 号"
4. **患者来了之后呢？** —— 进诊室，号状态推进，下一个号自动可叫

难点集中在第 3 条：**实时**。如果用轮询（前端每 5 秒查一次接口），简单但卡；用 WebSocket 推送，优雅但要新建配置。

**结论：必须用 WebSocket**，否则演示效果差、医生等不起。

---

## 二、要不要新建数据库？

**不需要新建库。** 在现有 `xikang_hospital` 库的 `register` 表上加字段即可。

理由：
- 叫号是 `register` 记录生命周期的一个阶段，**强归属**于挂号记录
- 拆独立表（如 `call_queue`）会导致每次查询都要 JOIN，得不偿失
- 加字段方案，历史挂号记录的新字段为 NULL，不影响旧数据

只需要：
- 加一个 `migrations/027_calling_fields.sql` 迁移脚本
- 同步改 `Register.java` / `RegistrationMapper.xml` 两处

---

## 三、数据模型设计

### 3.1 `register` 表新增字段

| 字段 | 类型 | 默认值 | 含义 |
|------|------|--------|------|
| `call_status` | `SMALLINT` | `0` | 号的呼叫状态（见下方枚举） |
| `called_time` | `TIMESTAMP` | NULL | 最近一次被叫的时刻（超时定时任务用此字段判断 5 分钟） |
| `answered_time` | `TIMESTAMP` | NULL | 患者应答（进诊室）的时刻 |
| `call_round` | `SMALLINT` | `0` | 已叫次数；`>= 2` 后过号为终态，禁止重叫 |

### 3.2 `call_status` 状态枚举

```
0  未叫     WAITING       已报到，等医生叫号
1  已叫     CALLED        医生刚叫，等患者进诊室
2  已应答   ANSWERED      患者进诊室，看诊中（visit_state 同步变 2）
3  过号     PASSED        叫了没人来，可重叫
```

### 3.3 状态机

```
                 报到成功
                    │
                    ▼
              ┌─────────────┐
              │ 0 未叫      │
              └─────────────┘
                    │ 医生叫号（call_round=1）
                    ▼
              ┌─────────────┐
       ┌───▶ │ 1 已叫      │◀─────────┐
       │     └─────────────┘          │
       │            │                  │
       │   ┌────────┼────────┐         │
       │   │        │        │         │ 医生重叫
       │   ▼        ▼        ▼         │ (仅 call_round<2)
       │ 过号    超时    应答           │
       │ (3)    (3)     (2)            │
       │   │     │        │            │
       │   │     │        ▼            │
       │   │     │   ┌─────────┐       │
       │   │     │   │ 已应答   │       │
       │   │     │   │ 看诊中  │       │
       │   │     │   └─────────┘       │
       │   │     │                     │
       │   └─────┴───┐                  │
       │            ▼                  │
       │  call_round < 2 ? ──── 否 ────┘
       │            │
       │            ▼ 是
       └────────────┘ (回到 1 已叫)

       call_round >= 2 时，过号为终态：
       状态保持 3，医生无法再次叫号，转人工
```

**关键约束**：
- `call_status=2 已应答` 时，必须同时把 `visit_state` 改为 `2 医生接诊`
- `call_status=1 已叫` 时，**不动 `visit_state`**，仍是 `1`（已挂号）
- `call_status=3 过号` 时，`visit_state` 仍是 `1`
- **过号上限 = 2**：`call_round >= 2` 后，状态保持 `3`，医生重叫会被 `CallingService` 拒绝（返回 400「该号已过号 2 次，请转人工处理」）
- 应答超时 5 分钟，由独立定时任务 `CallingTimeoutScheduler` 自动置为过号

---

## 四、接口设计

### 4.1 医生端接口（physician-service）

| 接口 | 方法 | 路径 | 作用 |
|------|------|------|------|
| 叫下一个 | POST | `/api/physician/call/next` | 自动选下一个号、置 `called` |
| 指定叫号 | POST | `/api/physician/call/{registerId}` | 叫指定号（重叫过号常用） |
| 患者应答 | POST | `/api/physician/call/{registerId}/answer` | 进诊室，置 `answered` |
| 标记过号 | POST | `/api/physician/call/{registerId}/pass` | 没人来，过号 |
| 当前叫号 | GET | `/api/physician/call/current` | 查当前医生名下"已叫未应答"的号 |

**请求示例**：
```
POST /api/physician/call/next
（无 body，医生身份从 PhysicianAuthContext 取）

Response:
{
  "code": 200,
  "data": {
    "registerId": 1023,
    "patientName": "张三",
    "queueNumber": 5,
    "callRound": 1,           // 第几次叫
    "calledTime": "2026-07-02T10:15:00"
  }
}
```

**"叫下一个"的选取规则**（重要）：
- 同一医生 (`employee_id`)、同一天 (`DATE(visit_date)=今天`)
- `check_in_time IS NOT NULL`（已报到）
- `call_status IN (0, 3)`（未叫或过号）
- 按 `check_in_time ASC`（先报到的先叫）
- 取第一条，更新为 `call_status=1, called_time=now, call_round+=1`

### 4.2 大屏 / 报到机查询接口（registration-service）

| 接口 | 方法 | 路径 | 作用 |
|------|------|------|------|
| 科室叫号板 | GET | `/api/registration/calling-board/{departmentId}` | 查某科室"已叫未应答"的所有号 |
| 全院叫号板 | GET | `/api/registration/calling-board` | 全院汇总（管理员大屏用） |

**响应示例**：
```json
{
  "code": 200,
  "data": [
    {
      "departmentName": "内科",
      "doctorName": "李医生",
      "currentCalling": "5",      // 当前叫到的号序
      "patientName": "张三",
      "callRound": 1,
      "calledTime": "2026-07-02T10:15:00",
      "waitingCount": 4           // 后面还有几人在等
    },
    ...
  ]
}
```

---

## 五、实时通信：WebSocket 设计

### 5.1 为什么不轮询

- 轮询：前端每 5 秒查一次 → 演示时医生叫号后大屏要等最多 5 秒才更新 → 体验差
- 轮询：所有候诊大屏都频繁打后端 → 高峰期 N 个诊室同时跑 → 接口压力大
- WebSocket：医生叫号后服务器**主动推**给所有订阅端 → 秒级、低开销

### 5.2 WebSocket 路径设计

```
/ws/calling/department/{departmentId}    订阅某科室叫号事件
/ws/calling/doctor/{doctorId}            订阅某医生叫号事件（报到机用）
/ws/calling/global                       全院订阅（管理员大屏用）
```

### 5.3 消息格式

医生叫号时，服务器向订阅了该科室/该医生的所有客户端推送：

```json
{
  "type": "CALLED",
  "data": {
    "registerId": 1023,
    "patientName": "张三",
    "queueNumber": 5,
    "doctorName": "李医生",
    "departmentName": "内科",
    "calledTime": "2026-07-02T10:15:00"
  }
}
```

事件类型：
- `CALLED` —— 医生叫号
- `ANSWERED` —— 患者应答
- `PASSED` —— 过号
- `RESET` —— 重置（管理员操作）

### 5.4 后端实现方式

用 Spring 的 `WebSocketHandler` 接口（不引 STOMP，保持轻量）：

```
registration-service/
└── config/
    └── CallingWebSocketConfig.java        # 注册 /ws/calling/** 路径
└── ws/
    └── CallingWebSocketHandler.java       # 维护 sessions Map
    └── CallingEventBroadcaster.java       # 业务侧调用，按主题推送
```

**关键决策**：叫号服务放在 `registration-service`，不新建微服务。
- 理由：叫号强依赖 `register` 表，与报到、爽约同源
- physician-service 通过 Feign 调 registration-service 的内部接口，触发广播

### 5.5 网关放行

`gateway-service` 的 `JwtAuthenticationFilter.java` 白名单加 `/ws/calling/`：
- 大屏是公共设备，不应要求登录
- 与现有 `/ws/voice` 保持一致的处理方式

---

## 六、前端页面设计

### 6.1 候诊大屏（新页面）

**路由**：`/calling-board/{departmentId}`（免登录，**一个科室一台大屏**）
**订阅**：`/ws/calling/department/{departmentId}`

**布局**：
```
┌──────────────────────────────────────────────┐
│         熙康云医院 · 候诊大屏                 │
│         当前时间：2026-07-02 10:15:33         │
├──────────────────────────────────────────────┤
│                                              │
│   ★ 现在叫号 ★                              │
│                                              │
│   ┌──────────────────────────────┐           │
│   │   内科 · 李医生               │           │
│   │                                │           │
│   │       5  号                   │  ← 巨大   │
│   │                                │           │
│   │   张三  请到 3 诊室           │           │
│   └──────────────────────────────┘           │
│                                              │
├──────────────────────────────────────────────┤
│   候诊队列                                    │
│   6 号 王五    7 号 赵六    8 号 钱七         │
└──────────────────────────────────────────────┘
```

- 字体要大（最小 48px），让 3 米外能看清
- WebSocket 推送 `CALLED` 事件时，"现在叫号"区域**闪一下 + 播放提示音**
- 候诊队列显示后续 3~5 个号

### 6.2 医生工作站改造

`PhysicianQueuePage.vue` 加两个按钮：

```
[ 叫下一个 ]  [ 标记过号 ]
```

- "叫下一个"调 `/api/physician/call/next`
- "标记过号"调 `/api/physician/call/{id}/pass`
- 患者列表里每行加 `call_status` 标签（未叫/已叫/过号）

### 6.3 报到机增强

`/test-checkin` 页（报到机）扫码后，除了显示自己的号序，**额外订阅 WebSocket**：
- 实时显示"当前叫到 X 号"
- 如果叫到自己 → 高亮 + 语音提示"请张三到 3 诊室"

---

## 七、实施步骤（推荐顺序）

按依赖关系排序，每步可独立验证：

### Phase 0：WebSocket 技术验证（半天，最先做）
0. 写一个 hello-world WS demo，跑通"浏览器 → gateway → registration-service"连通性
1. 若成功 → 继续 Phase 1
2. 若失败 → 把第五章实时通信方案改为 SSE，重写文档后重新评审

**这个阶段不做完，不要动后续代码。**

### Phase 1：数据库 + 实体（半天）
3. 写 `migrations/027_calling_fields.sql`
4. 同步改 `Register.java` 加 4 个字段
5. 改 `RegistrationMapper.xml` 的 `BaseResultMap` 和 `Base_Column_List`
6. 重启服务验证字段生效

### Phase 2：后端核心接口（1 天）
7. registration-service：写 `CallingService`（叫号业务逻辑，含 `call_round >= 2` 拒绝重叫校验）
8. registration-service：写 `CallingController`（4 个查询接口）
9. physician-service：写 Feign client 调 registration-service
10. physician-service：在 `PhysicianController` 加 4 个 action 接口
11. 新增 `CallingTimeoutScheduler` 定时任务（每分钟扫一次，5 分钟未应答自动过号）
12. Postman 跑通所有接口

### Phase 3：WebSocket 实时推送（1 天，依赖 Phase 0 通过）
13. registration-service：写 `CallingWebSocketConfig` + `CallingWebSocketHandler`
14. registration-service：写 `CallingEventBroadcaster`，在 `CallingService` 关键动作后调用
15. gateway 白名单加 `/ws/calling/`
16. 用浏览器开发者工具的 WS 面板验证推送

### Phase 4：前端大屏（1 天）
17. 新建 `CallingBoardPage.vue`，路由 `/calling-board/{departmentId}`
18. 接入 WebSocket 客户端，订阅 `/ws/calling/department/{id}`
19. 做大字布局 + 闪动动画 + 提示音

### Phase 5：医生工作站集成（半天）
20. `PhysicianQueuePage.vue` 加"叫下一个"和"标记过号"按钮
21. 接入 `/api/physician/call/*` 接口
22. 列表显示 `call_status` 标签（未叫/已叫/过号/过号终态）

### Phase 6：报到机增强（半天）
23. `/test-checkin` 加 WebSocket 订阅
24. 显示"当前叫号"
25. 加"点击启用语音"入口 + 叫到自己时高亮 + TTS 播报

**总工作量：4~4.5 天**（含 Phase 0 验证，不含完整联调测试）

---

## 八、关键设计决策清单

| 决策 | 选择 | 理由 |
|------|------|------|
| 新建数据库？ | ❌ 否 | 复用 `register` 表，加 4 个字段 |
| 新建微服务？ | ❌ 否 | 放在 `registration-service`，与报到同源 |
| 实时通信方式？ | WebSocket | 不轮询，秒级推送（Phase 3 先做技术验证） |
| WebSocket 协议？ | 裸 WS（无 STOMP） | 保持轻量，不引额外依赖 |
| 状态机：叫号是否改 `visit_state`？ | 仅"应答"改 | 叫号不算医生接诊，应答才算 |
| 过号处理 | 允许重叫，**上限 2 次** | `call_status=3` 可回 `1`，但 `call_round >= 2` 后锁定为终态转人工 |
| 应答超时 | **5 分钟**自动过号 | 新增 `CallingTimeoutScheduler` 定时任务 |
| 过号后队列顺序 | **不重排** | 保持 `check_in_time ASC`，避免"过号反而先看"的错觉 |
| 大屏部署粒度 | **一科室一大屏** | 路由 `/calling-board/{departmentId}`，全院汇总大屏作可选项 |
| 大屏需要登录吗？ | ❌ 不需要 | 公共设备，加白名单 |
| 多医生同时叫号会冲突吗？ | 不会 | 每个医生叫自己名下的号，互不干扰 |
| 报到机提示音 | **做** | 首次加载需用户点击启用（绕过浏览器自动播放限制） |

---

## 九、风险与未决问题

### 9.1 已识别的风险

1. **WebSocket 在 Spring Cloud Gateway 下的代理问题**
   - Gateway 默认是 Reactor Netty，对 WS 的支持需要确认配置
   - 备选：让大屏直连 registration-service（绕过网关），但破坏了统一鉴权
   - **Phase 3 第一天先做技术验证**：写一个 hello-world WS，跑通"前端 → 网关 → registration-service"的连通性。如果走不通，整个方案换 SSE（Server-Sent Events），文档需重写第五章

2. **多实例下 WebSocket session 共享**
   - 如果 registration-service 跑多实例，A 实例上的医生叫号，B 实例上的大屏收不到
   - 单实例开发环境无此问题；要上多实例需要 Redis Pub/Sub 转发
   - **当前阶段单实例，先不解决**，文档不展开

### 9.2 业务规则（已确认）

以下规则已经和需求方确认，作为后续实现的基线：

| 规则 | 决策 | 实现位置 |
|------|------|---------|
| **过号上限** | 同一号最多叫 **2 次**，第 2 次仍无人应答则锁定为"过号终态"，转人工处理 | `CallingService` 加校验：`call_round >= 2` 时拒绝再次叫号，状态保持 `3` |
| **应答超时** | 叫号后 **5 分钟** 未应答，定时任务自动置为过号 | 新增 `CallingTimeoutScheduler`，每分钟扫一次 `called_time` 早于 5 分钟且 `call_status=1` 的记录 |
| **大屏部署粒度** | **一台大屏一个科室** | 路由 `/calling-board/{departmentId}`；WebSocket 订阅 `/ws/calling/department/{departmentId}`；全院汇总大屏作为可选项放在 Phase 4 之后 |
| **报到机提示音** | **做**，但需处理浏览器自动播放限制 | 报到机页面首次加载时显示"点击启用语音"，用户点击后才能播放音频；叫到自己时播 TTS 或预录音频 |

### 9.3 过号后的队列处理（已确认）

- 过号的号**不自动重排**到队尾
- 顺序保持 `check_in_time ASC` 不变
- 医生可在工作站手动点"重叫"任意一个 `call_status=3` 的号
- 这样避免"过号的患者反而先看上"的错觉

---

## 十、文档维护

- 本文档完成后，所有后续实现以本文档为基线
- 任何状态机、接口、字段的变更，先改文档再改代码
- 实现过程中发现的 edge case，回填到本文档的"风险与未决问题"

---

**附：相关文件路径速查**

| 用途 | 路径 |
|------|------|
| 实体 | `registration-service/src/main/java/com/xikang/registration/entity/Register.java` |
| Mapper | `registration-service/src/main/resources/mapper/RegistrationMapper.xml` |
| 挂号服务 | `registration-service/src/main/java/com/xikang/registration/service/RegistrationService.java` |
| 爽约任务 | `registration-service/src/main/java/com/xikang/registration/scheduler/MissedAppointmentScheduler.java` |
| 医生工作站前端 | `xikang-hospital-frontend/src/modules/physician/pages/PhysicianQueuePage.vue` |
| 报到机 | `xikang-hospital-frontend/src/modules/registration/pages/ScanCheckinPage.vue` |
| 网关白名单 | `gateway-service/src/main/java/com/xikang/gateway/filter/JwtAuthenticationFilter.java` |
| Migration 目录 | `xikang-cloud-hospital/docker/migrations/` |

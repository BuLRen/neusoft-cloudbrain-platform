# 东软云医院系统 — 数据库详细设计文档

> 版本：v1.0
> 日期：2026-05-23
> 状态：初稿

---

## 1. 文档概述

### 1.1 编写目的

本文档是东软云医院系统数据库的详细设计文档，包含完整的 PostgreSQL DDL 语句（建表、约束、索引）、初始化数据脚本，以及每张表的设计说明。DBA 和后端开发人员可直接依据本文档中的 SQL 脚本创建数据库。

### 1.2 数据库环境

| 项目 | 值 |
| --- | --- |
| 数据库 | PostgreSQL 16.x |
| 数据库名 | xikang_hospital |
| Schema | public |
| 字符集 | UTF-8 |
| 排序规则 | zh_CN.UTF-8 |
| 时区 | Asia/Shanghai |

### 1.3 命名规范

| 对象 | 规范 | 示例 |
| --- | --- | --- |
| 表名 | 小写字母 + 下划线，名词单数 | `employee`, `register`, `ai_triage_record` |
| 字段名 | 小写字母 + 下划线 | `deptment_id`, `real_name`, `visit_state` |
| 主键 | `id` | 所有表统一使用 `id` 作为自增主键 |
| 外键列 | `关联表名_id` | `deptment_id`, `register_id`, `drug_id` |
| 索引名 | `idx_表名_字段名` | `idx_register_visit_state`, `idx_check_request_register_id` |
| 唯一索引 | `uk_表名_字段名` | `uk_department_dept_code`, `uk_disease_disease_icd` |
| 时间字段 | `xxx_time` / `xxx_date` | `creation_time`, `visit_date` |
| 状态字段 | `xxx_state` / `xxx_status` | `visit_state`, `check_state`, `plan_status` |
| 逻辑删除 | `delmark` | 1-正常/生效，0-已删除 |

### 1.4 数据表总览

本系统共 24 张数据表，分为两大类：

| 分类 | 表数量 | 表名列表 |
| --- | --- | --- |
| 基础业务表 | 15 | employee, department, regist_level, settle_category, scheduling, register, check_request, inspection_request, disposal_request, medical_technology, medical_record, medical_record_disease, disease, prescription, drug_info |
| AI 记录表 | 9 | ai_triage_record, ai_consultation_record, ai_medical_record_log, ai_exam_suggestion, ai_exam_analysis, ai_diagnosis_suggestion, ai_prescription_review, ai_follow_up_plan, ai_follow_up_record |

---

## 2. 数据库创建

```sql
-- ============================================================
-- 创建数据库
-- ============================================================
CREATE DATABASE xikang_hospital
    WITH
    ENCODING = 'UTF8'
    LC_COLLATE = 'zh_CN.UTF-8'
    LC_CTYPE = 'zh_CN.UTF-8'
    TEMPLATE = template0
    CONNECTION LIMIT = -1;

-- 连接到数据库后执行后续所有 DDL
\c xikang_hospital;

-- 设置时区
SET timezone = 'Asia/Shanghai';
```

---

## 3. 基础业务表 DDL

### 3.1 科室表 (department)

科室是系统的核心基础数据，被员工、挂号、医技项目等多张表引用。

```sql
-- ============================================================
-- 表: department (科室表)
-- 说明: 存储医院科室信息，如内科、外科、骨科等
-- ============================================================
CREATE TABLE department (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    dept_code       VARCHAR(64)     NOT NULL,                           -- 科室编码，如: SJNK
    dept_name       VARCHAR(64)     NOT NULL,                           -- 科室名称，如: 神经内科
    dept_type       VARCHAR(64)     DEFAULT NULL,                       -- 科室类型，如: 临床科室、医技科室
    delmark         SMALLINT        NOT NULL DEFAULT 1,                 -- 生效标记: 1-正常, 0-已删除

    -- 约束
    CONSTRAINT uk_department_dept_code UNIQUE (dept_code),
    CONSTRAINT chk_department_delmark CHECK (delmark IN (0, 1))
);

-- 索引
COMMENT ON TABLE department IS '科室表';
COMMENT ON COLUMN department.id IS '主键ID';
COMMENT ON COLUMN department.dept_code IS '科室编码，如: SJNK(神经内科)';
COMMENT ON COLUMN department.dept_name IS '科室名称';
COMMENT ON COLUMN department.dept_type IS '科室类型: 临床科室、医技科室等';
COMMENT ON COLUMN department.delmark IS '生效标记: 1-正常, 0-已删除';
```

### 3.2 挂号级别表 (regist_level)

```sql
-- ============================================================
-- 表: regist_level (挂号级别表)
-- 说明: 定义挂号等级和对应费用，如普通号、专家号、主任号
-- ============================================================
CREATE TABLE regist_level (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    regist_code     VARCHAR(64)     NOT NULL,                           -- 号别编码，如: PT, ZJ, ZR
    regist_name     VARCHAR(64)     NOT NULL,                           -- 号别名称，如: 普通号, 专家号
    regist_fee      DECIMAL(8,2)    NOT NULL DEFAULT 0.00,              -- 挂号费（元）
    regist_quota    INTEGER         DEFAULT 0,                          -- 每半天挂号限额
    sequence_no     INTEGER         DEFAULT 0,                          -- 显示顺序号
    delmark         SMALLINT        NOT NULL DEFAULT 1,                 -- 生效标记: 1-正常, 0-已删除

    -- 约束
    CONSTRAINT uk_regist_level_code UNIQUE (regist_code),
    CONSTRAINT chk_regist_level_fee CHECK (regist_fee >= 0),
    CONSTRAINT chk_regist_level_quota CHECK (regist_quota >= 0),
    CONSTRAINT chk_regist_level_delmark CHECK (delmark IN (0, 1))
);

COMMENT ON TABLE regist_level IS '挂号级别表';
COMMENT ON COLUMN regist_level.id IS '主键ID';
COMMENT ON COLUMN regist_level.regist_code IS '号别编码';
COMMENT ON COLUMN regist_level.regist_name IS '号别名称';
COMMENT ON COLUMN regist_level.regist_fee IS '挂号费（元）';
COMMENT ON COLUMN regist_level.regist_quota IS '每半天挂号限额';
COMMENT ON COLUMN regist_level.sequence_no IS '显示顺序号';
COMMENT ON COLUMN regist_level.delmark IS '生效标记: 1-正常, 0-已删除';
```

### 3.3 排班表 (scheduling)

```sql
-- ============================================================
-- 表: scheduling (排班表)
-- 说明: 医生出诊排班规则，按星期+午别编排
--       week_rule 格式: "1上2下3上" 表示周一上午、周二下午、周三上午出诊
-- ============================================================
CREATE TABLE scheduling (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    rule_name       VARCHAR(64)     NOT NULL,                           -- 排班规则名称，如: 周一三五上午
    week_rule       VARCHAR(14)     DEFAULT NULL,                       -- 星期规则，如: 1上3上5上
    delmark         SMALLINT        NOT NULL DEFAULT 1,                 -- 生效标记: 1-正常, 0-已删除

    CONSTRAINT chk_scheduling_delmark CHECK (delmark IN (0, 1))
);

COMMENT ON TABLE scheduling IS '排班规则表';
COMMENT ON COLUMN scheduling.week_rule IS '星期规则，格式: 数字+午别，如 1上3上5上 表示周一三五上午';
```

### 3.4 结算类别表 (settle_category)

```sql
-- ============================================================
-- 表: settle_category (结算类别表)
-- 说明: 患者的费用结算方式，如自费、医保、新农合
-- ============================================================
CREATE TABLE settle_category (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    settle_code     VARCHAR(64)     NOT NULL,                           -- 类别编码，如: ZF, YB
    settle_name     VARCHAR(64)     NOT NULL,                           -- 类别名称，如: 自费, 医保
    sequence_no     INTEGER         DEFAULT 0,                          -- 显示顺序号
    delmark         SMALLINT        NOT NULL DEFAULT 1,                 -- 生效标记: 1-正常, 0-已删除

    CONSTRAINT uk_settle_category_code UNIQUE (settle_code),
    CONSTRAINT chk_settle_category_delmark CHECK (delmark IN (0, 1))
);

COMMENT ON TABLE settle_category IS '结算类别表';
```

### 3.5 医院员工表 (employee)

```sql
-- ============================================================
-- 表: employee (医院员工表)
-- 说明: 存储医院员工信息，主要是坐诊医生，也包含挂号员、药师等
-- ============================================================
CREATE TABLE employee (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    deptment_id     INTEGER         DEFAULT NULL,                       -- 所在科室ID
    regist_level_id INTEGER         DEFAULT NULL,                       -- 挂号级别ID（仅医生有）
    scheduling_id   INTEGER         DEFAULT NULL,                       -- 排班规则ID（仅医生有）
    realname        VARCHAR(64)     NOT NULL,                           -- 真实姓名
    password        VARCHAR(64)     NOT NULL,                           -- 密码（BCrypt 加密存储）
    delmark         SMALLINT        NOT NULL DEFAULT 1,                 -- 生效标记: 1-生效, 0-已删除

    -- 外键约束
    CONSTRAINT fk_employee_department
        FOREIGN KEY (deptment_id) REFERENCES department(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_employee_regist_level
        FOREIGN KEY (regist_level_id) REFERENCES regist_level(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_employee_scheduling
        FOREIGN KEY (scheduling_id) REFERENCES scheduling(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_employee_delmark CHECK (delmark IN (0, 1))
);

-- 索引
CREATE INDEX idx_employee_deptment_id ON employee(deptment_id);
CREATE INDEX idx_employee_regist_level_id ON employee(regist_level_id);

COMMENT ON TABLE employee IS '医院员工表';
COMMENT ON COLUMN employee.password IS '密码（BCrypt加密存储，禁止明文）';
```

### 3.6 患者挂号信息表 (register)

这是系统业务流转的核心表，记录患者每次挂号的完整信息和看诊状态。

```sql
-- ============================================================
-- 表: register (患者历次挂号信息表)
-- 说明: 记录患者每次挂号的全部信息，是业务流转的核心表
-- ============================================================
CREATE TABLE register (
    id                      SERIAL          PRIMARY KEY,                -- 主键，自增
    case_number             VARCHAR(64)     NOT NULL,                   -- 病历号（全局唯一，跨次复用）
    real_name               VARCHAR(64)     NOT NULL,                   -- 患者姓名
    gender                  VARCHAR(6)      DEFAULT NULL,               -- 性别: 男/女
    card_number             VARCHAR(18)     DEFAULT NULL,               -- 身份证号
    birthdate               DATE            DEFAULT NULL,               -- 出生日期
    age                     INTEGER         DEFAULT NULL,               -- 年龄
    age_type                VARCHAR(6)      DEFAULT NULL,               -- 年龄类型: 年/天
    home_address            VARCHAR(128)    DEFAULT NULL,               -- 家庭住址（扩至128）
    visit_date              TIMESTAMP       DEFAULT NULL,               -- 本次看诊日期时间
    noon                    VARCHAR(6)      DEFAULT NULL,               -- 午别: 上午/下午
    deptment_id             INTEGER         NOT NULL,                   -- 本次挂号科室ID
    employee_id             INTEGER         NOT NULL,                   -- 本次挂号医生ID
    regist_level_id         INTEGER         NOT NULL,                   -- 本次挂号级别ID
    settle_category_id      INTEGER         DEFAULT NULL,               -- 结算类别ID
    is_book                 VARCHAR(2)      DEFAULT '否',               -- 病历本要否: 是/否
    regist_method           VARCHAR(10)     DEFAULT NULL,               -- 收费方式: 现金/银行卡/医保卡/微信/支付宝
    regist_money            DECIMAL(8,2)    DEFAULT 0.00,               -- 挂号金额
    visit_state             SMALLINT        NOT NULL DEFAULT 1,         -- 看诊状态:
                                                                        --   1-已挂号
                                                                        --   2-医生接诊
                                                                        --   3-看诊结束
                                                                        --   4-已退号

    -- 外键约束
    CONSTRAINT fk_register_department
        FOREIGN KEY (deptment_id) REFERENCES department(id),
    CONSTRAINT fk_register_employee
        FOREIGN KEY (employee_id) REFERENCES employee(id),
    CONSTRAINT fk_register_regist_level
        FOREIGN KEY (regist_level_id) REFERENCES regist_level(id),
    CONSTRAINT fk_register_settle_category
        FOREIGN KEY (settle_category_id) REFERENCES settle_category(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_register_gender CHECK (gender IS NULL OR gender IN ('男', '女')),
    CONSTRAINT chk_register_noon CHECK (noon IS NULL OR noon IN ('上午', '下午')),
    CONSTRAINT chk_register_is_book CHECK (is_book IN ('是', '否')),
    CONSTRAINT chk_register_visit_state CHECK (visit_state IN (1, 2, 3, 4)),
    CONSTRAINT chk_register_regist_money CHECK (regist_money >= 0)
);

-- 索引
CREATE INDEX idx_register_case_number ON register(case_number);
CREATE INDEX idx_register_real_name ON register(real_name);
CREATE INDEX idx_register_visit_state ON register(visit_state);
CREATE INDEX idx_register_employee_id ON register(employee_id);
CREATE INDEX idx_register_deptment_id ON register(deptment_id);
CREATE INDEX idx_register_visit_date ON register(visit_date);

COMMENT ON TABLE register IS '患者历次挂号信息表';
COMMENT ON COLUMN register.case_number IS '病历号，同一患者多次挂号共用同一病历号';
COMMENT ON COLUMN register.visit_state IS '看诊状态: 1-已挂号, 2-医生接诊, 3-看诊结束, 4-已退号';
```

### 3.7 医技项目表 (medical_technology)

```sql
-- ============================================================
-- 表: medical_technology (医技项目表)
-- 说明: 检查、检验、处置等医疗技术项目的统一目录
-- ============================================================
CREATE TABLE medical_technology (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    tech_code       VARCHAR(64)     NOT NULL,                           -- 项目编码，如: XJCT
    tech_name       VARCHAR(64)     NOT NULL,                           -- 项目名称，如: 胸部CT
    tech_format     VARCHAR(64)     DEFAULT NULL,                       -- 规格，如: 平扫、增强
    tech_price      DECIMAL(8,2)    NOT NULL DEFAULT 0.00,              -- 单价（元）
    tech_type       VARCHAR(64)     NOT NULL,                           -- 类型: check-检查 / inspection-检验 / disposal-处置
    price_type      VARCHAR(64)     DEFAULT NULL,                       -- 费用分类: 检查费/检验费/处置费
    deptment_id     INTEGER         DEFAULT NULL,                       -- 执行科室ID

    -- 外键约束
    CONSTRAINT fk_medtech_department
        FOREIGN KEY (deptment_id) REFERENCES department(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT uk_medtech_code UNIQUE (tech_code),
    CONSTRAINT chk_medtech_price CHECK (tech_price >= 0),
    CONSTRAINT chk_medtech_type CHECK (tech_type IN ('check', 'inspection', 'disposal'))
);

-- 索引
CREATE INDEX idx_medtech_type ON medical_technology(tech_type);
CREATE INDEX idx_medtech_deptment_id ON medical_technology(deptment_id);

COMMENT ON TABLE medical_technology IS '医技项目表（检查/检验/处置统一目录）';
COMMENT ON COLUMN medical_technology.tech_type IS '项目类型: check-检查, inspection-检验, disposal-处置';
```

### 3.8 检查申请表 (check_request)

```sql
-- ============================================================
-- 表: check_request (检查申请表)
-- 说明: 门诊医生开立的检查申请及检查结果
--       状态流转: 待检查 → 检查中 → 已完成
-- ============================================================
CREATE TABLE check_request (
    id                      SERIAL          PRIMARY KEY,                -- 主键，自增
    register_id             INTEGER         NOT NULL,                   -- 挂号记录ID
    medical_technology_id   INTEGER         NOT NULL,                   -- 医技项目ID
    check_info              VARCHAR(512)    DEFAULT NULL,               -- 目的要求
    check_position          VARCHAR(255)    DEFAULT NULL,               -- 检查部位
    creation_time           TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,  -- 开立时间
    check_employee_id       INTEGER         DEFAULT NULL,               -- 检查执行医生ID
    inputcheck_employee_id  INTEGER         DEFAULT NULL,               -- 结果录入医生ID
    check_time              TIMESTAMP       DEFAULT NULL,               -- 检查执行时间
    check_result            VARCHAR(512)    DEFAULT NULL,               -- 检查结果
    check_state             VARCHAR(64)     NOT NULL DEFAULT '待检查',  -- 状态: 待检查/检查中/已完成
    check_remark            VARCHAR(512)    DEFAULT NULL,               -- 备注

    -- 外键约束
    CONSTRAINT fk_check_request_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_check_request_medtech
        FOREIGN KEY (medical_technology_id) REFERENCES medical_technology(id),
    CONSTRAINT fk_check_request_employee
        FOREIGN KEY (check_employee_id) REFERENCES employee(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_check_request_input_employee
        FOREIGN KEY (inputcheck_employee_id) REFERENCES employee(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_check_request_state CHECK (check_state IN ('待检查', '检查中', '已完成'))
);

-- 索引
CREATE INDEX idx_check_request_register_id ON check_request(register_id);
CREATE INDEX idx_check_request_state ON check_request(check_state);
CREATE INDEX idx_check_request_medtech_id ON check_request(medical_technology_id);

COMMENT ON TABLE check_request IS '检查申请表';
COMMENT ON COLUMN check_request.check_state IS '状态: 待检查 → 检查中 → 已完成';
```

### 3.9 检验申请表 (inspection_request)

```sql
-- ============================================================
-- 表: inspection_request (检验申请表)
-- 说明: 门诊医生开立的检验申请及检验结果（一般有比对标准，如血常规）
--       状态流转: 待检验 → 检验中 → 已完成
-- ============================================================
CREATE TABLE inspection_request (
    id                          SERIAL          PRIMARY KEY,            -- 主键，自增
    register_id                 INTEGER         NOT NULL,               -- 挂号记录ID
    medical_technology_id       INTEGER         NOT NULL,               -- 医技项目ID
    inspection_info             VARCHAR(512)    DEFAULT NULL,           -- 目的要求
    inspection_position         VARCHAR(255)    DEFAULT NULL,           -- 检验部位
    creation_time               TIMESTAMP       DEFAULT CURRENT_TIMESTAMP, -- 开立时间
    inspection_employee_id      INTEGER         DEFAULT NULL,           -- 检验执行医生ID
    inputinspection_employee_id INTEGER         DEFAULT NULL,           -- 结果录入医生ID
    inspection_time             TIMESTAMP       DEFAULT NULL,           -- 检验执行时间
    inspection_result           VARCHAR(512)    DEFAULT NULL,           -- 检验结果
    inspection_state            VARCHAR(64)     NOT NULL DEFAULT '待检验', -- 状态: 待检验/检验中/已完成
    inspection_remark           VARCHAR(512)    DEFAULT NULL,           -- 备注

    -- 外键约束
    CONSTRAINT fk_inspection_request_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_inspection_request_medtech
        FOREIGN KEY (medical_technology_id) REFERENCES medical_technology(id),
    CONSTRAINT fk_inspection_request_employee
        FOREIGN KEY (inspection_employee_id) REFERENCES employee(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_inspection_request_input_employee
        FOREIGN KEY (inputinspection_employee_id) REFERENCES employee(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_inspection_state CHECK (inspection_state IN ('待检验', '检验中', '已完成'))
);

-- 索引
CREATE INDEX idx_inspection_request_register_id ON inspection_request(register_id);
CREATE INDEX idx_inspection_request_state ON inspection_request(inspection_state);

COMMENT ON TABLE inspection_request IS '检验申请表';
```

### 3.10 处置申请表 (disposal_request)

```sql
-- ============================================================
-- 表: disposal_request (处置申请表)
-- 说明: 门诊医生开立的处置申请及处置结果（一般有治疗作用，如洗胃）
--       状态流转: 待处置 → 处置中 → 已完成
-- ============================================================
CREATE TABLE disposal_request (
    id                          SERIAL          PRIMARY KEY,            -- 主键，自增
    register_id                 INTEGER         NOT NULL,               -- 挂号记录ID
    medical_technology_id       INTEGER         NOT NULL,               -- 医技项目ID
    disposal_info               VARCHAR(512)    DEFAULT NULL,           -- 目的要求
    disposal_position           VARCHAR(255)    DEFAULT NULL,           -- 处置部位
    creation_time               TIMESTAMP       DEFAULT CURRENT_TIMESTAMP, -- 开立时间
    disposal_employee_id        INTEGER         DEFAULT NULL,           -- 处置执行医生ID
    inputdisposal_employee_id   INTEGER         DEFAULT NULL,           -- 结果录入医生ID
    disposal_time               TIMESTAMP       DEFAULT NULL,           -- 处置执行时间
    disposal_result             VARCHAR(512)    DEFAULT NULL,           -- 处置结果
    disposal_state              VARCHAR(64)     NOT NULL DEFAULT '待处置', -- 状态: 待处置/处置中/已完成
    disposal_remark             VARCHAR(512)    DEFAULT NULL,           -- 备注

    -- 外键约束
    CONSTRAINT fk_disposal_request_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_disposal_request_medtech
        FOREIGN KEY (medical_technology_id) REFERENCES medical_technology(id),
    CONSTRAINT fk_disposal_request_employee
        FOREIGN KEY (disposal_employee_id) REFERENCES employee(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_disposal_request_input_employee
        FOREIGN KEY (inputdisposal_employee_id) REFERENCES employee(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_disposal_state CHECK (disposal_state IN ('待处置', '处置中', '已完成'))
);

-- 索引
CREATE INDEX idx_disposal_request_register_id ON disposal_request(register_id);
CREATE INDEX idx_disposal_request_state ON disposal_request(disposal_state);

COMMENT ON TABLE disposal_request IS '处置申请表';
```

### 3.11 疾病表 (disease)

```sql
-- ============================================================
-- 表: disease (疾病表)
-- 说明: 疾病字典表，包含 ICD 编码，供病历诊断和 AI 诊断使用
-- ============================================================
CREATE TABLE disease (
    id                  SERIAL          PRIMARY KEY,                    -- 主键，自增
    disease_code        VARCHAR(64)     DEFAULT NULL,                   -- 疾病助记编码，如: PJT(偏头痛)
    disease_name        VARCHAR(255)    NOT NULL,                       -- 疾病名称
    diseaseicd          VARCHAR(64)     DEFAULT NULL,                   -- 国际ICD编码，如: G43.9
    disease_category    VARCHAR(64)     DEFAULT NULL,                   -- 疾病所属分类，如: 神经系统疾病

    -- 约束
    CONSTRAINT uk_disease_icd UNIQUE (diseaseicd)
);

-- 索引
CREATE INDEX idx_disease_name ON disease(disease_name);
CREATE INDEX idx_disease_category ON disease(disease_category);

COMMENT ON TABLE disease IS '疾病字典表';
COMMENT ON COLUMN disease.diseaseicd IS '国际ICD编码，如: G43.9(偏头痛)';
```

### 3.12 患者病历表 (medical_record)

```sql
-- ============================================================
-- 表: medical_record (患者病历表)
-- 说明: 医生对患者的完整病历记录，包含主诉、现病史、诊断、处理意见等
--       每次挂号对应一条病历记录
-- ============================================================
CREATE TABLE medical_record (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    register_id     INTEGER         NOT NULL,                           -- 挂号记录ID（1:1 关系）
    readme          VARCHAR(512)    DEFAULT NULL,                       -- 主诉
    present         VARCHAR(512)    DEFAULT NULL,                       -- 现病史
    present_treat   VARCHAR(512)    DEFAULT NULL,                       -- 现病治疗情况
    history         VARCHAR(512)    DEFAULT NULL,                       -- 既往史
    allergy         VARCHAR(512)    DEFAULT NULL,                       -- 过敏史
    physique        VARCHAR(512)    DEFAULT NULL,                       -- 体格检查
    proposal        VARCHAR(512)    DEFAULT NULL,                       -- 检查/检验建议
    careful         VARCHAR(512)    DEFAULT NULL,                       -- 注意事项
    diagnosis       VARCHAR(512)    DEFAULT NULL,                       -- 诊断结果
    cure            VARCHAR(512)    DEFAULT NULL,                       -- 处理意见

    -- 外键约束
    CONSTRAINT fk_medical_record_register
        FOREIGN KEY (register_id) REFERENCES register(id),

    -- 唯一约束：每次挂号只有一条病历
    CONSTRAINT uk_medical_record_register UNIQUE (register_id)
);

COMMENT ON TABLE medical_record IS '患者病历表';
```

### 3.13 病历首页疾病关联表 (medical_record_disease)

```sql
-- ============================================================
-- 表: medical_record_disease (病历首页疾病关联表)
-- 说明: 病历与疾病的多对多关联，一条病历可关联多个初步诊断/确诊疾病
-- ============================================================
CREATE TABLE medical_record_disease (
    medical_record_id   INTEGER     NOT NULL,                           -- 病历ID
    disease_id          INTEGER     NOT NULL,                           -- 疾病ID

    -- 复合主键
    PRIMARY KEY (medical_record_id, disease_id),

    -- 外键约束
    CONSTRAINT fk_mrd_medical_record
        FOREIGN KEY (medical_record_id) REFERENCES medical_record(id)
        ON DELETE CASCADE,
    CONSTRAINT fk_mrd_disease
        FOREIGN KEY (disease_id) REFERENCES disease(id)
);

COMMENT ON TABLE medical_record_disease IS '病历-疾病关联表（多对多）';
```

### 3.14 药品信息表 (drug_info)

```sql
-- ============================================================
-- 表: drug_info (药品信息表)
-- 说明: 药品目录，包含药品编码、名称、规格、单价等信息
-- ============================================================
CREATE TABLE drug_info (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    drug_code       VARCHAR(255)    NOT NULL,                           -- 药品编码
    drug_name       VARCHAR(255)    NOT NULL,                           -- 药品名称
    drug_format     VARCHAR(255)    DEFAULT NULL,                       -- 药品规格，如: 100mg*30片/盒
    drug_unit       VARCHAR(16)     DEFAULT NULL,                       -- 包装单位，如: 盒、瓶、支
    manufacturer    VARCHAR(255)    DEFAULT NULL,                       -- 生产厂家
    drug_dosage     VARCHAR(64)     DEFAULT NULL,                       -- 药剂类型，如: 片剂、胶囊、注射剂
    drug_type       VARCHAR(64)     DEFAULT NULL,                       -- 药品类型: 西药/中成药/中草药
    drug_price      DECIMAL(8,2)    NOT NULL DEFAULT 0.00,              -- 药品单价（元）
    mnemonic_code   VARCHAR(255)    DEFAULT NULL,                       -- 拼音助记码，如: ASPL(阿司匹林)
    creation_date   DATE            DEFAULT CURRENT_DATE,               -- 创建时间

    -- 约束
    CONSTRAINT uk_drug_code UNIQUE (drug_code),
    CONSTRAINT chk_drug_price CHECK (drug_price >= 0)
);

-- 索引
CREATE INDEX idx_drug_name ON drug_info(drug_name);
CREATE INDEX idx_drug_mnemonic ON drug_info(mnemonic_code);
CREATE INDEX idx_drug_type ON drug_info(drug_type);

COMMENT ON TABLE drug_info IS '药品信息表';
COMMENT ON COLUMN drug_info.mnemonic_code IS '拼音助记码，用于快速搜索';
```

### 3.15 处方表 (prescription)

```sql
-- ============================================================
-- 表: prescription (处方表)
-- 说明: 医生开立的处方记录，每条记录对应一个药品
--       一次开方可能产生多条 prescription 记录
--       状态流转: 未发 → 已发 / 已退
-- ============================================================
CREATE TABLE prescription (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    register_id     INTEGER         NOT NULL,                           -- 挂号记录ID
    drug_id         INTEGER         NOT NULL,                           -- 药品ID
    drug_usage      VARCHAR(255)    DEFAULT NULL,                       -- 用法用量频次
    drug_number     VARCHAR(255)    DEFAULT NULL,                       -- 数量
    creation_time   TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,          -- 开立时间
    drug_state      VARCHAR(64)     NOT NULL DEFAULT '未发',            -- 状态: 未发/已发/已退

    -- 外键约束
    CONSTRAINT fk_prescription_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_prescription_drug
        FOREIGN KEY (drug_id) REFERENCES drug_info(id),

    -- 检查约束
    CONSTRAINT chk_prescription_state CHECK (drug_state IN ('未发', '已发', '已退'))
);

-- 索引
CREATE INDEX idx_prescription_register_id ON prescription(register_id);
CREATE INDEX idx_prescription_drug_id ON prescription(drug_id);
CREATE INDEX idx_prescription_state ON prescription(drug_state);

COMMENT ON TABLE prescription IS '处方表（每条记录对应一个药品）';
COMMENT ON COLUMN prescription.drug_state IS '状态: 未发 → 已发，或 未发 → 已退';
```

---

## 4. AI 记录表 DDL

### 4.1 AI 导诊记录表 (ai_triage_record)

```sql
-- ============================================================
-- 表: ai_triage_record (AI 导诊记录表)
-- 说明: 记录患者挂号前的 AI 智能导诊过程和推荐结果
-- ============================================================
CREATE TABLE ai_triage_record (
    id                      SERIAL          PRIMARY KEY,                -- 主键，自增
    patient_name            VARCHAR(64)     DEFAULT NULL,               -- 患者姓名（挂号前可能无档案）
    patient_age             INTEGER         DEFAULT NULL,               -- 患者年龄
    patient_gender          VARCHAR(6)      DEFAULT NULL,               -- 患者性别
    symptom_description     TEXT            NOT NULL,                   -- 症状描述原文
    recommend_dept_id       INTEGER         DEFAULT NULL,               -- AI推荐科室ID
    recommend_dept_name     VARCHAR(64)     DEFAULT NULL,               -- AI推荐科室名称（冗余）
    recommend_doctor_id     INTEGER         DEFAULT NULL,               -- AI推荐医生ID（可为空）
    recommend_doctor_name   VARCHAR(64)     DEFAULT NULL,               -- AI推荐医生姓名（冗余）
    risk_level              VARCHAR(16)     NOT NULL DEFAULT 'normal',  -- 风险等级: normal/urgent/critical
    is_priority             SMALLINT        NOT NULL DEFAULT 0,         -- 是否优先接诊: 0-否, 1-是
    ai_analysis             TEXT            DEFAULT NULL,               -- AI分析说明
    register_id             INTEGER         DEFAULT NULL,               -- 关联挂号ID（实际挂号后回填）
    triage_time             TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,  -- 导诊时间
    model_id                VARCHAR(64)     DEFAULT NULL,               -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_triage_dept
        FOREIGN KEY (recommend_dept_id) REFERENCES department(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_ai_triage_doctor
        FOREIGN KEY (recommend_doctor_id) REFERENCES employee(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_ai_triage_register
        FOREIGN KEY (register_id) REFERENCES register(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_ai_triage_gender CHECK (patient_gender IS NULL OR patient_gender IN ('男', '女')),
    CONSTRAINT chk_ai_triage_risk CHECK (risk_level IN ('normal', 'urgent', 'critical')),
    CONSTRAINT chk_ai_triage_priority CHECK (is_priority IN (0, 1))
);

-- 索引
CREATE INDEX idx_ai_triage_register_id ON ai_triage_record(register_id);
CREATE INDEX idx_ai_triage_risk_level ON ai_triage_record(risk_level);
CREATE INDEX idx_ai_triage_time ON ai_triage_record(triage_time);

COMMENT ON TABLE ai_triage_record IS 'AI导诊记录表';
COMMENT ON COLUMN ai_triage_record.register_id IS '关联挂号ID，患者实际挂号后回填';
```

### 4.2 AI 预问诊记录表 (ai_consultation_record)

```sql
-- ============================================================
-- 表: ai_consultation_record (AI 预问诊记录表)
-- 说明: 记录患者挂号后的 AI 预问诊多轮对话过程和最终摘要
--       每轮对话一条记录，最终轮次包含完整的问诊摘要
-- ============================================================
CREATE TABLE ai_consultation_record (
    id                      SERIAL          PRIMARY KEY,                -- 主键，自增
    register_id             INTEGER         NOT NULL,                   -- 挂号记录ID
    round_number            INTEGER         NOT NULL DEFAULT 1,         -- 问诊轮次
    ai_question             TEXT            DEFAULT NULL,               -- AI提问内容
    patient_answer          TEXT            DEFAULT NULL,               -- 患者回答内容
    consultation_state      VARCHAR(16)     NOT NULL DEFAULT 'in_progress', -- 问诊状态
    chief_complaint         VARCHAR(512)    DEFAULT NULL,               -- 主诉摘要
    symptom_duration        VARCHAR(128)    DEFAULT NULL,               -- 症状持续时间
    history_summary         TEXT            DEFAULT NULL,               -- 既往病史摘要
    allergy_summary         TEXT            DEFAULT NULL,               -- 过敏史摘要
    medication_summary      TEXT            DEFAULT NULL,               -- 用药情况摘要
    ai_summary              TEXT            DEFAULT NULL,               -- AI问诊总结
    suggested_exam          TEXT            DEFAULT NULL,               -- 推荐检查方向
    creation_time           TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,  -- 创建时间
    completion_time         TIMESTAMP       DEFAULT NULL,               -- 完成时间
    model_id                VARCHAR(64)     DEFAULT NULL,               -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_consult_register
        FOREIGN KEY (register_id) REFERENCES register(id),

    -- 检查约束
    CONSTRAINT chk_ai_consult_state CHECK (consultation_state IN ('in_progress', 'completed', 'cancelled'))
);

-- 索引
CREATE INDEX idx_ai_consult_register_id ON ai_consultation_record(register_id);
CREATE INDEX idx_ai_consult_state ON ai_consultation_record(consultation_state);

COMMENT ON TABLE ai_consultation_record IS 'AI预问诊记录表（多轮对话）';
```

### 4.3 AI 病历生成日志表 (ai_medical_record_log)

```sql
-- ============================================================
-- 表: ai_medical_record_log (AI 病历生成日志表)
-- 说明: 记录 AI 自动生成病历的请求、生成内容和医生的修改记录
-- ============================================================
CREATE TABLE ai_medical_record_log (
    id                  SERIAL          PRIMARY KEY,                    -- 主键，自增
    register_id         INTEGER         NOT NULL,                       -- 挂号记录ID
    medical_record_id   INTEGER         DEFAULT NULL,                   -- 病历ID（生成后关联）
    source_type         VARCHAR(32)     NOT NULL,                       -- 生成来源: consultation/dictation/exam
    ai_readme           TEXT            DEFAULT NULL,                   -- AI生成的主诉
    ai_present          TEXT            DEFAULT NULL,                   -- AI生成的现病史
    ai_history          TEXT            DEFAULT NULL,                   -- AI生成的既往史
    ai_allergy          TEXT            DEFAULT NULL,                   -- AI生成的过敏史
    ai_physique         TEXT            DEFAULT NULL,                   -- AI生成的体格检查
    ai_diagnosis        TEXT            DEFAULT NULL,                   -- AI生成的初步诊断
    is_adopted          SMALLINT        NOT NULL DEFAULT 0,             -- 采纳状态: 0-未采纳/1-部分/2-完全
    doctor_modification TEXT            DEFAULT NULL,                   -- 医生修改内容（JSON）
    generation_time     TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,      -- 生成时间
    model_id            VARCHAR(64)     DEFAULT NULL,                   -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_mrlog_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_ai_mrlog_medical_record
        FOREIGN KEY (medical_record_id) REFERENCES medical_record(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_ai_mrlog_source CHECK (source_type IN ('consultation', 'dictation', 'exam')),
    CONSTRAINT chk_ai_mrlog_adopted CHECK (is_adopted IN (0, 1, 2))
);

-- 索引
CREATE INDEX idx_ai_mrlog_register_id ON ai_medical_record_log(register_id);

COMMENT ON TABLE ai_medical_record_log IS 'AI病历生成日志表';
COMMENT ON COLUMN ai_medical_record_log.doctor_modification IS '医生修改内容，JSON格式记录被修改的字段和修改前后值';
```

### 4.4 AI 检查推荐表 (ai_exam_suggestion)

```sql
-- ============================================================
-- 表: ai_exam_suggestion (AI 检查推荐表)
-- 说明: AI 根据患者病历自动推荐的检查/检验项目
-- ============================================================
CREATE TABLE ai_exam_suggestion (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    register_id     INTEGER         NOT NULL,                           -- 挂号记录ID
    tech_id         INTEGER         NOT NULL,                           -- 推荐的医技项目ID
    tech_name       VARCHAR(64)     DEFAULT NULL,                       -- 项目名称（冗余）
    suggest_type    VARCHAR(16)     NOT NULL,                           -- 推荐类型: check/inspection
    suggest_reason  TEXT            DEFAULT NULL,                       -- 推荐理由
    priority        INTEGER         NOT NULL DEFAULT 1,                 -- 优先级: 1-高/2-中/3-低
    is_adopted      SMALLINT        NOT NULL DEFAULT 0,                 -- 是否采纳: 0-未采纳/1-已采纳
    creation_time   TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,          -- 生成时间
    model_id        VARCHAR(64)     DEFAULT NULL,                       -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_exam_sug_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_ai_exam_sug_medtech
        FOREIGN KEY (tech_id) REFERENCES medical_technology(id),

    -- 检查约束
    CONSTRAINT chk_ai_exam_sug_type CHECK (suggest_type IN ('check', 'inspection')),
    CONSTRAINT chk_ai_exam_sug_priority CHECK (priority IN (1, 2, 3)),
    CONSTRAINT chk_ai_exam_sug_adopted CHECK (is_adopted IN (0, 1))
);

-- 索引
CREATE INDEX idx_ai_exam_sug_register_id ON ai_exam_suggestion(register_id);

COMMENT ON TABLE ai_exam_suggestion IS 'AI检查/检验推荐表';
```

### 4.5 AI 检查结果分析表 (ai_exam_analysis)

```sql
-- ============================================================
-- 表: ai_exam_analysis (AI 检查结果分析表)
-- 说明: AI 对检查/检验结果的智能分析报告
--       由检查结果录入后自动触发生成
-- ============================================================
CREATE TABLE ai_exam_analysis (
    id                      SERIAL          PRIMARY KEY,                -- 主键，自增
    register_id             INTEGER         NOT NULL,                   -- 挂号记录ID
    check_request_id        INTEGER         DEFAULT NULL,               -- 检查申请ID（检查类）
    inspection_request_id   INTEGER         DEFAULT NULL,               -- 检验申请ID（检验类）
    analysis_type           VARCHAR(16)     NOT NULL,                   -- 分析类型: check/inspection
    original_result         TEXT            DEFAULT NULL,               -- 原始结果数据
    abnormal_indicators     TEXT            DEFAULT NULL,               -- 异常指标（JSON）
    risk_level              VARCHAR(16)     NOT NULL DEFAULT 'normal',  -- 风险等级
    analysis_report         TEXT            DEFAULT NULL,               -- AI分析报告
    correlation_analysis    TEXT            DEFAULT NULL,               -- 与病历关联分析
    is_viewed               SMALLINT        NOT NULL DEFAULT 0,         -- 医生是否查看: 0/1
    analysis_time           TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,  -- 分析时间
    model_id                VARCHAR(64)     DEFAULT NULL,               -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_exam_analysis_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_ai_exam_analysis_check
        FOREIGN KEY (check_request_id) REFERENCES check_request(id)
        ON DELETE SET NULL,
    CONSTRAINT fk_ai_exam_analysis_inspection
        FOREIGN KEY (inspection_request_id) REFERENCES inspection_request(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_ai_exam_analysis_type CHECK (analysis_type IN ('check', 'inspection')),
    CONSTRAINT chk_ai_exam_analysis_risk CHECK (risk_level IN ('normal', 'attention', 'warning', 'danger')),
    CONSTRAINT chk_ai_exam_analysis_viewed CHECK (is_viewed IN (0, 1))
);

-- 索引
CREATE INDEX idx_ai_exam_analysis_register_id ON ai_exam_analysis(register_id);
CREATE INDEX idx_ai_exam_analysis_check_id ON ai_exam_analysis(check_request_id);
CREATE INDEX idx_ai_exam_analysis_inspection_id ON ai_exam_analysis(inspection_request_id);
CREATE INDEX idx_ai_exam_analysis_risk ON ai_exam_analysis(risk_level);

COMMENT ON TABLE ai_exam_analysis IS 'AI检查/检验结果分析表';
COMMENT ON COLUMN ai_exam_analysis.abnormal_indicators IS '异常指标，JSON格式，如: [{"指标":"白细胞","值":"15.2","参考值":"4-10","单位":"10^9/L"}]';
```

### 4.6 AI 诊断建议表 (ai_diagnosis_suggestion)

```sql
-- ============================================================
-- 表: ai_diagnosis_suggestion (AI 诊断建议表)
-- 说明: AI 根据病历+检查结果推荐的疑似疾病列表
--       一次诊断可能推荐多种疾病，按概率排序
-- ============================================================
CREATE TABLE ai_diagnosis_suggestion (
    id                  SERIAL          PRIMARY KEY,                    -- 主键，自增
    register_id         INTEGER         NOT NULL,                       -- 挂号记录ID
    disease_id          INTEGER         DEFAULT NULL,                   -- 推荐疾病ID
    disease_name        VARCHAR(255)    DEFAULT NULL,                   -- 推荐疾病名称（冗余）
    recommend_icd       VARCHAR(64)     DEFAULT NULL,                   -- 推荐ICD编码
    probability         DECIMAL(5,2)    DEFAULT NULL,                   -- 疾病概率(百分比)
    risk_level          VARCHAR(16)     NOT NULL DEFAULT 'low',         -- 风险等级: low/medium/high
    treatment_direction TEXT            DEFAULT NULL,                   -- 推荐治疗方向
    diagnosis_basis     TEXT            DEFAULT NULL,                   -- 诊断依据
    is_adopted          SMALLINT        NOT NULL DEFAULT 0,             -- 是否采纳: 0/1
    sort_order          INTEGER         NOT NULL DEFAULT 1,             -- 排序序号（按概率）
    creation_time       TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,      -- 生成时间
    model_id            VARCHAR(64)     DEFAULT NULL,                   -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_diagnosis_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_ai_diagnosis_disease
        FOREIGN KEY (disease_id) REFERENCES disease(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_ai_diagnosis_risk CHECK (risk_level IN ('low', 'medium', 'high')),
    CONSTRAINT chk_ai_diagnosis_adopted CHECK (is_adopted IN (0, 1)),
    CONSTRAINT chk_ai_diagnosis_probability CHECK (probability IS NULL OR (probability >= 0 AND probability <= 100))
);

-- 索引
CREATE INDEX idx_ai_diagnosis_register_id ON ai_diagnosis_suggestion(register_id);
CREATE INDEX idx_ai_diagnosis_disease_id ON ai_diagnosis_suggestion(disease_id);

COMMENT ON TABLE ai_diagnosis_suggestion IS 'AI辅助诊断建议表';
COMMENT ON COLUMN ai_diagnosis_suggestion.probability IS '疾病概率百分比，如85.50表示85.50%';
```

### 4.7 AI 处方审核表 (ai_prescription_review)

```sql
-- ============================================================
-- 表: ai_prescription_review (AI 处方审核表)
-- 说明: AI 对医生处方的自动审核结果，检测药物冲突/过敏/剂量异常
-- ============================================================
CREATE TABLE ai_prescription_review (
    id              SERIAL          PRIMARY KEY,                        -- 主键，自增
    register_id     INTEGER         NOT NULL,                           -- 挂号记录ID
    prescription_id INTEGER         NOT NULL,                           -- 处方ID
    review_result   VARCHAR(16)     NOT NULL,                           -- 审核结果: passed/warning/rejected
    drug_conflict   TEXT            DEFAULT NULL,                       -- 药物冲突检测（JSON）
    allergy_risk    TEXT            DEFAULT NULL,                       -- 过敏风险检测（JSON）
    duplicate_drug  TEXT            DEFAULT NULL,                       -- 重复用药检测（JSON）
    dosage_check    TEXT            DEFAULT NULL,                       -- 剂量合理性检测（JSON）
    risk_score      INTEGER         NOT NULL DEFAULT 0,                -- 综合风险评分(0-100)
    risk_details    TEXT            DEFAULT NULL,                       -- 风险详情（自然语言）
    doctor_action   VARCHAR(16)     DEFAULT NULL,                       -- 医生处理: accepted/overridden
    review_time     TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,          -- 审核时间
    model_id        VARCHAR(64)     DEFAULT NULL,                       -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_review_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_ai_review_prescription
        FOREIGN KEY (prescription_id) REFERENCES prescription(id),

    -- 检查约束
    CONSTRAINT chk_ai_review_result CHECK (review_result IN ('passed', 'warning', 'rejected')),
    CONSTRAINT chk_ai_review_score CHECK (risk_score >= 0 AND risk_score <= 100),
    CONSTRAINT chk_ai_review_action CHECK (doctor_action IS NULL OR doctor_action IN ('accepted', 'overridden'))
);

-- 索引
CREATE INDEX idx_ai_review_register_id ON ai_prescription_review(register_id);
CREATE INDEX idx_ai_review_prescription_id ON ai_prescription_review(prescription_id);
CREATE INDEX idx_ai_review_result ON ai_prescription_review(review_result);

COMMENT ON TABLE ai_prescription_review IS 'AI处方审核表';
COMMENT ON COLUMN ai_prescription_review.risk_score IS '综合风险评分0-100，0无风险，100极高风险';
```

### 4.8 AI 用药随访计划表 (ai_follow_up_plan)

```sql
-- ============================================================
-- 表: ai_follow_up_plan (AI 用药随访计划表)
-- 说明: 患者取药后 AI 自动生成的随访计划
--       由药房发药成功后自动触发
-- ============================================================
CREATE TABLE ai_follow_up_plan (
    id                  SERIAL          PRIMARY KEY,                    -- 主键，自增
    register_id         INTEGER         NOT NULL,                       -- 挂号记录ID
    prescription_id     INTEGER         DEFAULT NULL,                   -- 处方ID
    follow_up_day       INTEGER         DEFAULT NULL,                   -- 随访天数（第几天）
    planned_date        DATE            DEFAULT NULL,                   -- 计划随访日期
    follow_up_type      VARCHAR(32)     NOT NULL,                       -- 随访类型
    content_template    TEXT            DEFAULT NULL,                   -- 随访内容模板
    plan_status         VARCHAR(16)     NOT NULL DEFAULT 'pending',     -- 状态: pending/completed/overdue/cancelled
    creation_time       TIMESTAMP       DEFAULT CURRENT_TIMESTAMP,      -- 创建时间
    model_id            VARCHAR(64)     DEFAULT NULL,                   -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_followup_register
        FOREIGN KEY (register_id) REFERENCES register(id),
    CONSTRAINT fk_ai_followup_prescription
        FOREIGN KEY (prescription_id) REFERENCES prescription(id)
        ON DELETE SET NULL,

    -- 检查约束
    CONSTRAINT chk_ai_followup_type CHECK (follow_up_type IN ('medication', 'side_effect', 'recovery', 'revisit')),
    CONSTRAINT chk_ai_followup_status CHECK (plan_status IN ('pending', 'completed', 'overdue', 'cancelled'))
);

-- 索引
CREATE INDEX idx_ai_followup_register_id ON ai_follow_up_plan(register_id);
CREATE INDEX idx_ai_followup_status ON ai_follow_up_plan(plan_status);
CREATE INDEX idx_ai_followup_planned_date ON ai_follow_up_plan(planned_date);

COMMENT ON TABLE ai_follow_up_plan IS 'AI用药随访计划表';
COMMENT ON COLUMN ai_follow_up_plan.follow_up_type IS '随访类型: medication-用药跟踪, side_effect-副作用反馈, recovery-康复评估, revisit-复诊提醒';
```

### 4.9 AI 随访记录表 (ai_follow_up_record)

```sql
-- ============================================================
-- 表: ai_follow_up_record (AI 随访记录表)
-- 说明: 患者用药随访的反馈记录和 AI 康复评估
-- ============================================================
CREATE TABLE ai_follow_up_record (
    id                  SERIAL          PRIMARY KEY,                    -- 主键，自增
    follow_up_plan_id   INTEGER         NOT NULL,                       -- 随访计划ID
    register_id         INTEGER         NOT NULL,                       -- 挂号记录ID
    is_on_time          SMALLINT        DEFAULT NULL,                   -- 是否按时服药: 0/1/NULL
    side_effect         TEXT            DEFAULT NULL,                   -- 副作用描述
    has_side_effect     SMALLINT        DEFAULT NULL,                   -- 是否出现副作用: 0/1/NULL
    symptom_relief      VARCHAR(32)     DEFAULT NULL,                   -- 症状缓解: relieved/partial/unchanged/worsened
    need_revisit        SMALLINT        DEFAULT NULL,                   -- 是否需要复诊: 0/1/NULL
    patient_feedback    TEXT            DEFAULT NULL,                   -- 患者补充反馈
    ai_assessment       TEXT            DEFAULT NULL,                   -- AI康复评估
    ai_advice           TEXT            DEFAULT NULL,                   -- AI健康建议
    follow_up_time      TIMESTAMP       DEFAULT NULL,                   -- 随访完成时间
    model_id            VARCHAR(64)     DEFAULT NULL,                   -- AI模型标识

    -- 外键约束
    CONSTRAINT fk_ai_followup_record_plan
        FOREIGN KEY (follow_up_plan_id) REFERENCES ai_follow_up_plan(id),
    CONSTRAINT fk_ai_followup_record_register
        FOREIGN KEY (register_id) REFERENCES register(id),

    -- 检查约束
    CONSTRAINT chk_ai_fur_ontime CHECK (is_on_time IS NULL OR is_on_time IN (0, 1)),
    CONSTRAINT chk_ai_fur_side_effect CHECK (has_side_effect IS NULL OR has_side_effect IN (0, 1)),
    CONSTRAINT chk_ai_fur_relief CHECK (symptom_relief IS NULL OR symptom_relief IN ('relieved', 'partial', 'unchanged', 'worsened')),
    CONSTRAINT chk_ai_fur_revisit CHECK (need_revisit IS NULL OR need_revisit IN (0, 1))
);

-- 索引
CREATE INDEX idx_ai_fur_plan_id ON ai_follow_up_record(follow_up_plan_id);
CREATE INDEX idx_ai_fur_register_id ON ai_follow_up_record(register_id);

COMMENT ON TABLE ai_follow_up_record IS 'AI随访反馈记录表';
```

---

## 5. 索引汇总

### 5.1 业务表索引

| 表 | 索引名 | 索引列 | 类型 | 用途 |
| --- | --- | --- | --- | --- |
| department | uk_department_dept_code | dept_code | UNIQUE | 科室编码唯一 |
| employee | idx_employee_deptment_id | deptment_id | NORMAL | 按科室查医生 |
| employee | idx_employee_regist_level_id | regist_level_id | NORMAL | 按级别查医生 |
| register | idx_register_case_number | case_number | NORMAL | 按病历号查挂号 |
| register | idx_register_real_name | real_name | NORMAL | 按姓名查挂号 |
| register | idx_register_visit_state | visit_state | NORMAL | 按状态筛选患者 |
| register | idx_register_employee_id | employee_id | NORMAL | 医生查自己的患者 |
| register | idx_register_deptment_id | deptment_id | NORMAL | 按科室查挂号 |
| register | idx_register_visit_date | visit_date | NORMAL | 按日期查挂号 |
| regist_level | uk_regist_level_code | regist_code | UNIQUE | 级别编码唯一 |
| settle_category | uk_settle_category_code | settle_code | UNIQUE | 类别编码唯一 |
| medical_technology | uk_medtech_code | tech_code | UNIQUE | 项目编码唯一 |
| medical_technology | idx_medtech_type | tech_type | NORMAL | 按类型筛选 |
| medical_technology | idx_medtech_deptment_id | deptment_id | NORMAL | 按科室筛选 |
| check_request | idx_check_request_register_id | register_id | NORMAL | 按挂号查检查 |
| check_request | idx_check_request_state | check_state | NORMAL | 按状态筛选 |
| check_request | idx_check_request_medtech_id | medical_technology_id | NORMAL | 按项目筛选 |
| inspection_request | idx_inspection_request_register_id | register_id | NORMAL | 按挂号查检验 |
| inspection_request | idx_inspection_request_state | inspection_state | NORMAL | 按状态筛选 |
| disposal_request | idx_disposal_request_register_id | register_id | NORMAL | 按挂号查处置 |
| disposal_request | idx_disposal_request_state | disposal_state | NORMAL | 按状态筛选 |
| disease | uk_disease_icd | diseaseicd | UNIQUE | ICD编码唯一 |
| disease | idx_disease_name | disease_name | NORMAL | 按名称搜索 |
| disease | idx_disease_category | disease_category | NORMAL | 按分类筛选 |
| medical_record | uk_medical_record_register | register_id | UNIQUE | 每次挂号一条病历 |
| drug_info | uk_drug_code | drug_code | UNIQUE | 药品编码唯一 |
| drug_info | idx_drug_name | drug_name | NORMAL | 按名称搜索 |
| drug_info | idx_drug_mnemonic | mnemonic_code | NORMAL | 按助记码搜索 |
| drug_info | idx_drug_type | drug_type | NORMAL | 按类型筛选 |
| prescription | idx_prescription_register_id | register_id | NORMAL | 按挂号查处方 |
| prescription | idx_prescription_drug_id | drug_id | NORMAL | 按药品查处方 |
| prescription | idx_prescription_state | drug_state | NORMAL | 按状态筛选 |

### 5.2 AI 表索引

| 表 | 索引名 | 索引列 | 类型 | 用途 |
| --- | --- | --- | --- | --- |
| ai_triage_record | idx_ai_triage_register_id | register_id | NORMAL | 按挂号查导诊记录 |
| ai_triage_record | idx_ai_triage_risk_level | risk_level | NORMAL | 按风险等级筛选 |
| ai_triage_record | idx_ai_triage_time | triage_time | NORMAL | 按时间查询 |
| ai_consultation_record | idx_ai_consult_register_id | register_id | NORMAL | 按挂号查问诊记录 |
| ai_consultation_record | idx_ai_consult_state | consultation_state | NORMAL | 按状态筛选 |
| ai_medical_record_log | idx_ai_mrlog_register_id | register_id | NORMAL | 按挂号查生成记录 |
| ai_exam_suggestion | idx_ai_exam_sug_register_id | register_id | NORMAL | 按挂号查推荐 |
| ai_exam_analysis | idx_ai_exam_analysis_register_id | register_id | NORMAL | 按挂号查分析 |
| ai_exam_analysis | idx_ai_exam_analysis_check_id | check_request_id | NORMAL | 按检查申请查分析 |
| ai_exam_analysis | idx_ai_exam_analysis_inspection_id | inspection_request_id | NORMAL | 按检验申请查分析 |
| ai_exam_analysis | idx_ai_exam_analysis_risk | risk_level | NORMAL | 按风险等级筛选 |
| ai_diagnosis_suggestion | idx_ai_diagnosis_register_id | register_id | NORMAL | 按挂号查诊断 |
| ai_diagnosis_suggestion | idx_ai_diagnosis_disease_id | disease_id | NORMAL | 按疾病查推荐 |
| ai_prescription_review | idx_ai_review_register_id | register_id | NORMAL | 按挂号查审核 |
| ai_prescription_review | idx_ai_review_prescription_id | prescription_id | NORMAL | 按处方查审核 |
| ai_prescription_review | idx_ai_review_result | review_result | NORMAL | 按审核结果筛选 |
| ai_follow_up_plan | idx_ai_followup_register_id | register_id | NORMAL | 按挂号查随访 |
| ai_follow_up_plan | idx_ai_followup_status | plan_status | NORMAL | 按状态筛选 |
| ai_follow_up_plan | idx_ai_followup_planned_date | planned_date | NORMAL | 按日期查到期随访 |
| ai_follow_up_record | idx_ai_fur_plan_id | follow_up_plan_id | NORMAL | 按计划查反馈 |
| ai_follow_up_record | idx_ai_fur_register_id | register_id | NORMAL | 按挂号查反馈 |

---

## 6. 约束汇总

### 6.1 CHECK 约束

| 表 | 约束名 | 约束条件 | 说明 |
| --- | --- | --- | --- |
| department | chk_department_delmark | delmark IN (0,1) | 逻辑删除标记 |
| regist_level | chk_regist_level_fee | regist_fee >= 0 | 费用不能为负 |
| regist_level | chk_regist_level_quota | regist_quota >= 0 | 限额不能为负 |
| register | chk_register_gender | gender IN ('男','女') | 性别枚举 |
| register | chk_register_noon | noon IN ('上午','下午') | 午别枚举 |
| register | chk_register_is_book | is_book IN ('是','否') | 是否枚举 |
| register | chk_register_visit_state | visit_state IN (1,2,3,4) | 看诊状态枚举 |
| register | chk_register_regist_money | regist_money >= 0 | 金额不能为负 |
| medical_technology | chk_medtech_price | tech_price >= 0 | 单价不能为负 |
| medical_technology | chk_medtech_type | tech_type IN ('check','inspection','disposal') | 项目类型枚举 |
| check_request | chk_check_request_state | check_state IN ('待检查','检查中','已完成') | 检查状态枚举 |
| inspection_request | chk_inspection_state | inspection_state IN ('待检验','检验中','已完成') | 检验状态枚举 |
| disposal_request | chk_disposal_state | disposal_state IN ('待处置','处置中','已完成') | 处置状态枚举 |
| prescription | chk_prescription_state | drug_state IN ('未发','已发','已退') | 处方状态枚举 |
| drug_info | chk_drug_price | drug_price >= 0 | 药品单价不能为负 |
| ai_triage_record | chk_ai_triage_risk | risk_level IN ('normal','urgent','critical') | 风险等级枚举 |
| ai_consultation_record | chk_ai_consult_state | consultation_state IN ('in_progress','completed','cancelled') | 问诊状态枚举 |
| ai_medical_record_log | chk_ai_mrlog_source | source_type IN ('consultation','dictation','exam') | 生成来源枚举 |
| ai_medical_record_log | chk_ai_mrlog_adopted | is_adopted IN (0,1,2) | 采纳状态枚举 |
| ai_exam_suggestion | chk_ai_exam_sug_type | suggest_type IN ('check','inspection') | 推荐类型枚举 |
| ai_exam_suggestion | chk_ai_exam_sug_priority | priority IN (1,2,3) | 优先级枚举 |
| ai_exam_analysis | chk_ai_exam_analysis_risk | risk_level IN ('normal','attention','warning','danger') | 分析风险等级 |
| ai_diagnosis_suggestion | chk_ai_diagnosis_risk | risk_level IN ('low','medium','high') | 诊断风险等级 |
| ai_diagnosis_suggestion | chk_ai_diagnosis_probability | probability 0~100 | 概率范围 |
| ai_prescription_review | chk_ai_review_result | review_result IN ('passed','warning','rejected') | 审核结果枚举 |
| ai_prescription_review | chk_ai_review_score | risk_score 0~100 | 评分范围 |
| ai_follow_up_plan | chk_ai_followup_type | follow_up_type IN (...) | 随访类型枚举 |
| ai_follow_up_plan | chk_ai_followup_status | plan_status IN ('pending','completed','overdue','cancelled') | 计划状态枚举 |
| ai_follow_up_record | chk_ai_fur_relief | symptom_relief IN ('relieved','partial','unchanged','worsened') | 缓解情况枚举 |

### 6.2 UNIQUE 约束

| 表 | 约束名 | 约束列 | 说明 |
| --- | --- | --- | --- |
| department | uk_department_dept_code | dept_code | 科室编码全局唯一 |
| regist_level | uk_regist_level_code | regist_code | 号别编码全局唯一 |
| settle_category | uk_settle_category_code | settle_code | 类别编码全局唯一 |
| medical_technology | uk_medtech_code | tech_code | 项目编码全局唯一 |
| disease | uk_disease_icd | diseaseicd | ICD编码全局唯一 |
| medical_record | uk_medical_record_register | register_id | 每次挂号只能有一条病历 |
| drug_info | uk_drug_code | drug_code | 药品编码全局唯一 |

### 6.3 FOREIGN KEY 约束

| 从表 | 外键列 | 引用表 | 引用列 | 删除策略 |
| --- | --- | --- | --- | --- |
| employee | deptment_id | department | id | SET NULL |
| employee | regist_level_id | regist_level | id | SET NULL |
| employee | scheduling_id | scheduling | id | SET NULL |
| register | deptment_id | department | id | RESTRICT |
| register | employee_id | employee | id | RESTRICT |
| register | regist_level_id | regist_level | id | RESTRICT |
| register | settle_category_id | settle_category | id | SET NULL |
| check_request | register_id | register | id | RESTRICT |
| check_request | medical_technology_id | medical_technology | id | RESTRICT |
| check_request | check_employee_id | employee | id | SET NULL |
| check_request | inputcheck_employee_id | employee | id | SET NULL |
| inspection_request | register_id | register | id | RESTRICT |
| inspection_request | medical_technology_id | medical_technology | id | RESTRICT |
| inspection_request | inspection_employee_id | employee | id | SET NULL |
| inspection_request | inputinspection_employee_id | employee | id | SET NULL |
| disposal_request | register_id | register | id | RESTRICT |
| disposal_request | medical_technology_id | medical_technology | id | RESTRICT |
| disposal_request | disposal_employee_id | employee | id | SET NULL |
| disposal_request | inputdisposal_employee_id | employee | id | SET NULL |
| medical_record | register_id | register | id | RESTRICT |
| medical_record_disease | medical_record_id | medical_record | id | CASCADE |
| medical_record_disease | disease_id | disease | id | RESTRICT |
| prescription | register_id | register | id | RESTRICT |
| prescription | drug_id | drug_info | id | RESTRICT |
| ai_triage_record | recommend_dept_id | department | id | SET NULL |
| ai_triage_record | recommend_doctor_id | employee | id | SET NULL |
| ai_triage_record | register_id | register | id | SET NULL |
| ai_consultation_record | register_id | register | id | RESTRICT |
| ai_medical_record_log | register_id | register | id | RESTRICT |
| ai_medical_record_log | medical_record_id | medical_record | id | SET NULL |
| ai_exam_suggestion | register_id | register | id | RESTRICT |
| ai_exam_suggestion | tech_id | medical_technology | id | RESTRICT |
| ai_exam_analysis | register_id | register | id | RESTRICT |
| ai_exam_analysis | check_request_id | check_request | id | SET NULL |
| ai_exam_analysis | inspection_request_id | inspection_request | id | SET NULL |
| ai_diagnosis_suggestion | register_id | register | id | RESTRICT |
| ai_diagnosis_suggestion | disease_id | disease | id | SET NULL |
| ai_prescription_review | register_id | register | id | RESTRICT |
| ai_prescription_review | prescription_id | prescription | id | RESTRICT |
| ai_follow_up_plan | register_id | register | id | RESTRICT |
| ai_follow_up_plan | prescription_id | prescription | id | SET NULL |
| ai_follow_up_record | follow_up_plan_id | ai_follow_up_plan | id | RESTRICT |
| ai_follow_up_record | register_id | register | id | RESTRICT |

> **删除策略说明**：
> - `RESTRICT`：有子记录时禁止删除父记录（业务数据强依赖，如挂号引用科室时不允许删除科室）
> - `SET NULL`：删除父记录时将外键置空（弱依赖，如员工引用科室，科室删除后员工科室置空）
> - `CASCADE`：删除父记录时级联删除子记录（如删除病历时级联删除病历-疾病关联）

---

## 7. ER 关系图

```
department (1) ──┬──< (N) employee
                 ├──< (N) register
                 └──< (N) medical_technology

regist_level (1) ────< (N) register
                 └──< (N) employee

scheduling (1) ──────< (N) employee

settle_category (1) ─< (N) register

employee (1) ────┬──< (N) register (挂号医生)
                 ├──< (N) check_request (检查医生)
                 ├──< (N) inspection_request (检验医生)
                 └──< (N) disposal_request (处置医生)

register (1) ────┬──< (1) medical_record
                 ├──< (N) check_request
                 ├──< (N) inspection_request
                 ├──< (N) disposal_request
                 ├──< (N) prescription
                 ├──< (N) ai_consultation_record
                 ├──< (N) ai_medical_record_log
                 ├──< (N) ai_exam_suggestion
                 ├──< (N) ai_exam_analysis
                 ├──< (N) ai_diagnosis_suggestion
                 ├──< (N) ai_prescription_review
                 ├──< (N) ai_follow_up_plan
                 └──< (N) ai_follow_up_record

medical_record (1) ──< (N) medical_record_disease >── (N) ── disease (1)

drug_info (1) ───────< (N) prescription

medical_technology (1) ──┬──< (N) check_request
                         ├──< (N) inspection_request
                         ├──< (N) disposal_request
                         └──< (N) ai_exam_suggestion

disease (1) ────────────< (N) ai_diagnosis_suggestion

prescription (1) ──┬──< (N) ai_prescription_review
                   └──< (N) ai_follow_up_plan

check_request (1) ──────< (N) ai_exam_analysis
inspection_request (1) ─< (N) ai_exam_analysis

ai_follow_up_plan (1) ──< (N) ai_follow_up_record
```

---

## 8. 建表顺序

由于外键依赖关系，建表必须按以下顺序执行：

```
第 1 层（无外键依赖）:
  1. department
  2. regist_level
  3. scheduling
  4. settle_category
  5. disease
  6. drug_info
  7. medical_technology

第 2 层（依赖第 1 层）:
  8. employee

第 3 层（依赖第 1、2 层）:
  9. register

第 4 层（依赖第 3 层）:
  10. medical_record
  11. check_request
  12. inspection_request
  13. disposal_request
  14. prescription

第 5 层（依赖第 4 层）:
  15. medical_record_disease

第 6 层（AI 表，依赖第 3、4 层）:
  16. ai_triage_record
  17. ai_consultation_record
  18. ai_medical_record_log
  19. ai_exam_suggestion
  20. ai_exam_analysis
  21. ai_diagnosis_suggestion
  22. ai_prescription_review
  23. ai_follow_up_plan

第 7 层（依赖第 6 层）:
  24. ai_follow_up_record
```

---

> **文档维护说明**：本文档中的 DDL 脚本可直接在 PostgreSQL 16.x 上执行。任何表结构变更（增删字段、修改约束、调整索引）必须同步更新本文档。

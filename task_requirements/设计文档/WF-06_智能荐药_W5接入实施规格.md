# WF-06 智能荐药 W5 接入实施规格（handoff）

> **用途**：指导 Dify WF-06 编排与前后端联调。  
> **前提**：`migrate_022_ai_drug_suggestion.sql` 已执行；`INTERNAL_AI_TOKEN` 已配置；**`drug_info` 已按设计文档第 3 节导入 NMPA 本位码数据（国产+进口）**。  
> **设计文档**：[`WF-06_智能荐药_Dify工作流设计文档.md`](WF-06_智能荐药_Dify工作流设计文档.md)

---

## 0. 任务一句话

将 Dify WF-06「智能荐药」工作流接入 `physician-service` 与 `xikang-hospital-frontend`，使开立处方页能：触发 W5 → 解析 End 节点输出 → 写入 `ai_drug_suggestion` → 前端展示并支持医生采纳进处方篮。

---

## 1. 已完成（代码侧）

| 类别 | 内容 | 关键路径 |
|------|------|----------|
| DB | `ai_drug_suggestion` 表 | `docker/init-db/migrate_022_ai_drug_suggestion.sql` |
| 药品检索 API | `POST /api/physician/internal/drugs/ai-search` | **`ai-catalog-service`**（云 :8098）`CatalogInternalController.java`、`DrugAiSearchService.java` |
| InputBuilder | W5 Start 11 字段 | `W5DifyInputBuilder.java` |
| OutputMapper | End 7 字段 | `DifyW5OutputMapper.java` |
| Pipeline | `runW5` / `persistW5` / Fallback | `PhysicianAiPipelineService.java` |
| API | `POST /w5/recommend-drugs`、`GET /w5/suggestions/{id}` | `PhysicianAiController.java` |
| 前端 | W5 面板 + 采纳 | `W5PrescriptionPanel.vue`、`PhysicianPrescriptionPage.vue` |
| 配置占位 | `DIFY_API_KEY_W5`、`DIFY_WORKFLOW_W5` | `.env.example`、`application.yml` |

**未配置 `DIFY_API_KEY_W5` 时**：自动走 `FallbackWorkflowEngine.runW5`，页面仍可演示荐药与采纳。

---

## 2. Dify 控制台编排步骤（1.11.2）

1. 新建 **Workflow App**，命名建议：`WF-06 智能荐药`。
2. 按设计文档 **第 5 节** 逐节点创建（10 节点 + 双 End）。
3. HTTP 节点 5 配置：
   - URL: `http://172.17.0.1:8098/api/physician/internal/drugs/ai-search`（Docker 内访问 **ai-catalog-service**）
   - Header: `Authorization: Bearer <INTERNAL_AI_TOKEN>`
   - Body: `{{构造药品检索请求.search_request_body}}`
4. 发布工作流，复制 **API Key**（`app-xxx`）到 `.env`：

```bash
DIFY_WORKFLOW_W5=true
DIFY_API_KEY_W5=app-xxxxxxxx
```

5. 重启 `physician-service`，日志应出现：`W5 智能荐药：已启用 Dify Workflow`。

---

## 3. 环境变量清单

| 变量 | 说明 |
|------|------|
| `DIFY_ENABLED` | `true` |
| `DIFY_BASE_URL` | 自托管 Dify 根地址（无 `/v1`） |
| `DIFY_WORKFLOW_W5` | `true` / `1` / `yes` / `on` |
| `DIFY_API_KEY_W5` | WF-06 Workflow App 专用 Key |
| `INTERNAL_AI_TOKEN` | **ai-catalog-service（云）** HTTP 节点回调鉴权 |

---

## 4. 联调检查清单

- [ ] TC-001：有确诊 + 常见病 → `status=success`，suggestions 含 `drugId`
- [ ] TC-002：青霉素过敏 → 推荐列表排除相关药物
- [ ] TC-003：药品库无匹配 → `status=fallback`
- [ ] TC-004：无确诊 → 后端返回「请先完成门诊确诊」
- [ ] TC-005：进口中成药可出现在候选/推荐中（若已导入进口本位码表）
- [ ] TC-006：HTTP 检索失败时走 fallback，工作流不中断
- [ ] 采纳 → 处方篮出现药品 → 提交处方 → `prescription.diagnosis` 有值
- [ ] `drugCode` 为 14 位 NMPA 本位码（非 `SEED010` / `AMX001` 演示码）

---

## 5. 与 W4 差异速查

| 项 | W4 确诊 | W5 荐药 |
|----|---------|---------|
| 工作流编号 | WF-05 | WF-06 |
| 检索 API | `/internal/diseases/ai-search` | `/internal/drugs/ai-search` |
| 落库表 | `ai_diagnosis_suggestion` | `ai_drug_suggestion` |
| 医生操作 | 采纳确诊 | 采纳进处方篮 |
| 前置条件 | 建议完成 W3 | **必须**有确诊病名 |

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/xikang_hospital
    username: postgres
    password: postgres
  ai:
    openai:
      api-key: your-api-key
      base-url: https://api.openai.com

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xikang.registration.mapper.ExpenseRecordMapper">

    <resultMap id="BaseResultMap" type="com.xikang.registration.entity.ExpenseRecord">
        <id column="id" property="id"/>
        <result column="register_id" property="registerId"/>
        <result column="patient_id" property="patientId"/>
        <result column="patient_name" property="patientName"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="item_id" property="itemId"/>
        <result column="item_name" property="itemName"/>
        <result column="item_code" property="itemCode"/>
        <result column="quantity" property="quantity"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="pay_time" property="payTime"/>
        <result column="refund_time" property="refundTime"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, register_id, patient_id, patient_name, category_id, category_name, item_id, item_name,
        item_code, quantity, unit_price, total_amount, status, pay_time, refund_time,
        operator_id, operator_name, remark, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record WHERE id = #{id}
    </select>

    <select id="selectByRegisterId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record WHERE register_id = #{registerId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByPatientId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record WHERE patient_id = #{patientId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingByRegisterId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record
        WHERE register_id = #{registerId} AND status = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingByPatientId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record
        WHERE patient_id = #{patientId} AND status = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingByRegisterAndIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record
        WHERE register_id = #{registerId}
        AND status = 0
        <if test="itemIds != null and itemIds.size() > 0">
            AND item_id IN
            <foreach collection="itemIds" item="itemId" open="(" separator="," close=")">
                #{itemId}
            </foreach>
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectUnrefundedByRegisterId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM expense_record
        WHERE register_id = #{registerId} AND status != 2
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO expense_record (
            register_id, patient_id, patient_name, category_id, category_name, item_id, item_name,
            item_code, quantity, unit_price, total_amount, status, pay_time, operator_id, operator_name, remark
        ) VALUES (
            #{registerId}, #{patientId}, #{patientName}, #{categoryId}, #{categoryName}, #{itemId}, #{itemName},
            #{itemCode}, #{quantity}, #{unitPrice}, #{totalAmount}, #{status}, #{payTime},
            #{operatorId}, #{operatorName}, #{remark}
        )
    </insert>

    <update id="update">
        UPDATE expense_record
        SET status = #{status},
            pay_time = #{payTime},
            refund_time = #{refundTime},
            operator_id = #{operatorId},
            operator_name = #{operatorName},
            remark = #{remark}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE expense_record SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updatePayStatus">
        UPDATE expense_record SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
